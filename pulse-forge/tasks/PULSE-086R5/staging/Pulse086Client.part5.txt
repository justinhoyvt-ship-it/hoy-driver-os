div><div class="r4-req-meta"><span class="r4-req-fare">'+esc(fare(r.quotedFare)||'Fare pending')+'</span><span class="r4-req-pay">'+esc(pay(r))+'</span></div>';var a=document.createElement('div');a.className='inbox-actions';var y=button(timing(r)==='NOW'?(S().active?'Accept · queue':'Accept'):'Accept · schedule','inbox-accept',function(){decide(r,'CONFIRM',y)}),n=button('Decline','inbox-decline',function(){decide(r,'DECLINE',n)});a.appendChild(y);a.appendChild(n);c.appendChild(a);l.appendChild(c)})}
function startScheduled(s,b){if(!S().shift){say('Start shift first');return}var r=schedRide(s);if(S().active){enqueue(r);say('Scheduled ride queued next');renderSched();return}b.classList.add('r4-busy');beginPickup(r).then(function(){return route(r.pickup,'Pickup')}).catch(function(e){say(e.message||'Could not start pickup')}).then(function(){b.classList.remove('r4-busy');refresh()})}
function renderSched(){var l=E('schedList');if(!l)return;l.innerHTML='';var a=S().active,list=R.scheduled.filter(function(s){return!R.queue.some(function(q){return q.requestId&&q.requestId===s.requestId})&&!(a&&a.requestId===s.requestId)});if(!list.length){l.innerHTML='<div style="padding:4px;color:var(--r4m)">No upcoming rides</div>';return}list.forEach(function(s){var c=document.createElement('div');c.className='r4-sched';c.innerHTML='<div><div class="r4-time">'+esc(dt(s.whenISO))+'</div><b>'+esc(s.name||'Pulse rider')+'</b><div class="r4-route2">'+esc((s.pickup||'Pickup')+' → '+(s.dest||s.destination||'Destination'))+'</div><div class="r4-payline">'+esc(fare(s.quotedFare)+' · '+pay(s))+'</div></div>';var b=button(a?'Queue next':'Start pickup','',function(){startScheduled(s,b)});b.disabled=!S().shift;c.appendChild(b);l.appendChild(c)})}
function install(){window.requestReviewLocked_=function(){return false};window.openDecision=function(){say('Use Inbox Accept / Decline')};window.renderInbox=renderInbox;window.renderSched=renderSched;window.startScheduled=function(id){var s=R.scheduled.filter(function(x){return x.id===id})[0];if(s)startScheduled(s,button('','',function(){}))};window.dropOff=finish}
function boot(){restore();try{install()}catch(e){}if(!mount()){setTimeout(boot,250);return}refresh();setInterval(function(){try{install();save();paint();renderSched();if(E('inboxOverlay')&&E('inboxOverlay').classList.contains('show'))renderInbox()}catch(e){console.warn('Pulse R4 paint',e)}},1500);setInterval(refresh,8000);window.addEventListener('focus',refresh);window.PulseDriverR5={build:R.build,state:function(){return{mounted:R.mounted,shift:S().shift,active:S().active,queue:R.queue.slice(),requests:R.requests.slice(),scheduled:R.scheduled.slice()}}}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
</script>
