nted');return false}}
function clock(){var s=S(),sec=Math.max(0,Math.floor((Date.now()-Number(s.shift.startedAt))/1000)),h=Math.floor(sec/3600),m=Math.floor((sec%3600)/60),z=sec%60;return(h?h+':':'')+(h&&m<10?'0':'')+m+':'+(z<10?'0':'')+z}
function toggleShift(){S().shift?endShift():startShift()} function startShift(){var s=S();if(s.shift)return;s.shift={startedAt:Date.now(),miles:0,rides:0,earned:0,tips:0,requestIds:[]};save();say('Shift started');paint()}
function endShift(){var s=S();if(!s.shift||R.endBusy)return;if(s.active||R.queue.length){say(s.active&&s.active.state==='ON_TRIP'?'Complete the active ride before ending shift':'Cancel or complete the active pickup before ending shift');return}var e=Date.now(),x=s.shift,p={startISO:new Date(x.startedAt).toISOString(),endISO:new Date(e).toISOString(),onlineHours:Math.max(.0001,(e-x.startedAt)/3600000),miles:Number(x.miles)||0,earnings:'',tips:'',zone:'',notes:(x.rides||0)+' Pulse rides · earnings pending'};R.endBusy=true;paint();srv('endShiftToSheet',p).then(function(){s.shift=null;save();say('Shift ended')}).catch(function(z){say(z.message||'Could not end shift')}).then(function(){R.endBusy=false;paint()})}
function button(t,c,f){var b=document.createElement('button');b.type='button';b.textContent=t;b.className=c||'';b.onclick=f;return b}
function routeOrigin(){try{var mm=W('meMarker');if(mm&&mm.getLatLng){var x=mm.getLatLng();return [Number(x.lat),Number(x.lng)]}var mp=W('map');if(mp&&mp.getCenter){var c=mp.getCenter();return [Number(c.lat),Number(c.lng)]}}catch(e){}return [null,null]}
function clearRoute(){try{var mp=W('map');if(mp&&R.routeLine){mp.removeLayer(R.routeLine);R.routeLine=null}if(mp&&R.routeMarker){mp.removeLayer(R.routeMarker);R.routeMarker=null}}catch(e){}}
function route(a,label){a=String(a||'').trim();if(!a){say('Address unavailable');return Promise.resolve(null)}if(R.routeBusy)return Promise.resolve(null);R.routeBusy=true;var o=routeOrigin();return srv('pulse085bRoutePreview',o[0],o[1],a).then(function(res){R.routeBusy=false;if(!res||res.ok!==true)throw new Error('Route unavailable');clearRoute();var mp=W('map'),leaf=W('L'),pts=Array.isArray(res.routePoints)?res.routePoints:[],dest=Array.isArray(res.destinationPoint)?res.destinationPoint:null;try{if(mp&&leaf&&pts.length>1){R.routeLine=leaf.polyline(pts,{color:'#43d9ea',weight:5,opacity:.9}).addTo(mp);mp.fitBounds(R.routeLine.getBounds(),{padding:[28,28],maxZoom:15})}else if(mp&&dest&&isFinite(dest[0])&&isFinite(dest[1]))mp.setView(dest,15);if(mp&&leaf&&dest&&isFinite(dest[0])&&isFinite(dest[1]))R.routeMarker=leaf.circleMarker(dest,{radius:7,color:'#ffc45d',weight:3,fillColor:'#ffc45d',fillOpacity:1}).addTo(mp)}catch(e){}var summary=[res.durationText,res.distanceText].filter(Boolean).join(' · ');say((label||'Route')+(summary?' · '+summary:''));return res}).catch(function(e){R.routeBusy=false;say(e&&e.message?e.message:'Route unavailable');return null})}
function beginPickup(r){if(!S().shift)return Promise.reject(new Error('Start shift first'));return r.requestId?srv('beginForegroundPickup',r.requestId,r.reservationId||r.requestId,r.pickup||'').then(function(x){r.riderStatus=x&&x.status||'Leaving';r.routeOpened=false;S().active=r;save();paint();return r}):(Promise.resolve().then(function(){r.routeOpened=false;S().active=r;save();paint();return r}))}
function enqueue(r){if(!r)return;if((S().active&&S().active.requestId===r.requestId)||R.queue.some(function(x){return x.requestId&&x.requestId===r.requestId}))return;r.state='QUEUED';R.queue.push(r);save();paint()}
function promote(){if(R.promoting||S().active||!S().shift||!R.queue.length)return;R.promoting=true;var r=R.queue.shift();r.state='ACCEPTED';save();beginPickup(r).catch(function(e){R.queue.unshift(r);save();say(e.message||'Could not start next pickup')}).then(function(){R.promoting=false;paint()})}
function riderStatus(next,done){var a=S().active;if(!a||!a.requestId){done();return}srv('updateRiderStatus',a.requestId,next,a.requestId+':'+next.toLowerCase().replace(/\s+/g,'-')).then(function(){a.riderStatus=next;save();done()}).catch(function(e){say(e.message||'Status update failed')})}
function advanceToRideStart(a){var seq=['Confirmed','Leaving','On the way','Arriving soon','Arrived','Ride in progress'],cur=stat(a),i=seq.indexOf(cur);if(i<0)i=0;var p=Promise.resolve();for(var n=i+1;n<seq.length;n++){(function(next){p=p.then(function(){return srv('updateRiderStatus',a.requestId,next,a.requestId+':'+next.toLowerCase().replace(/\s+/g,'-')).then(function(){a.riderStatus=next;save();paint()})})})(seq[n])}return p}
function startRide(){var a=S().active;if(!a)return;function go(){a.state='ON_TRIP';a.pickedUpAt=Date.now();a.riderStatus='Ride in progress';var f=W('startTrace');if(typeof f==='function')f();save();paint();route(a.dest,'Destination')}if(!a.requestId){go();return}advanceToRideStart(a).then(go).catch(function(e){say(e&&e.message?e.message:'Could not start ride')})}
function finish(){var s=S(),a=s.active;if(!a)return;function don