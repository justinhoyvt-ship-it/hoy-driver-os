tent=Number(quote.distanceMiles||0).toFixed(1)+' mi · '+Number(quote.durationMinutes||0)+' min';$('quoteResult').classList.add('show');$('quoteState').textContent='Fare ready.';$('quoteState').classList.remove('busy');updateSend()}).withFailureHandler(function(e){fareFail((e&&e.message)||'Fare unavailable.')}).pulseGetFareQuote({pickup:$('pickup').value.trim(),destination:$('destination').value.trim(),pickupAt:pickupISO(),requestToken:requestToken,testMode:testMode})}
  function fareFail(m){quote=null;quoteFingerprint='';$('quoteResult').classList.remove('show');$('quoteState').textContent='Fare unavailable. Check the locations and retry.';$('quoteState').classList.remove('busy');$('quoteBtn').classList.add('show');updateSend();error(m)}$('quoteBtn').onclick=calcFare;
  function smart(id){var input=$(id),list=$(id+'Suggestions'),hint=$(id+'Hint');input.addEventListener('input',function(){if(id==='pickup'){pickupLat='';pickupLng=''}invalidate();clearTimeout(suggestionTimers[id]);var v=input.value.trim();if(v.length<3){list.innerHTML='';hint.textContent='Start typing an address or place.';return}hint.textContent='Finding nearby matches…';hint.classList.add('busy');suggestionTimers[id]=setTimeout(function(){var seq=++suggestionSeq[id];google.script.run.withSuccessHandler(function(r){if(seq!==suggestionSeq[id])return;list.innerHTML='';((r&&r.suggestions)||[]).forEach(function(x){var o=document.createElement('option');o.value=x.label;list.appendChild(o)});hint.textContent=list.children.length?'Choose a match or keep typing.':'No match yet — keep typing.';hint.classList.remove('busy')}).withFailureHandler(function(){hint.textContent='Keep typing; the route check will verify it.';hint.classList.remove('busy')}).pulseSmartAddressSuggestions({query:v,requestToken:requestToken,testMode:testMode})},280);scheduleFare()});input.addEventListener('change',function(){scheduleFare(200)})}
  smart('pickup');smart('destination');$('time').addEventListener('input',function(){invalidate();scheduleFare()});
  $('locationBtn').onclick=function(){clearError();if(!navigator.geolocation)return error('Location is not available on this phone.');var b=this;b.disabled=true;b.textContent='Locating…';navigator.geolocation.getCurrentPosition(function(p){pickupLat=Number(p.coords.latitude).toFixed(6);pickupLng=Number(p.coords.longitude).toFixed(6);google.script.run.withSuccessHandler(function(r){$('pickup').value=r&&r.label?r.label:(pickupLat+', '+pickupLng);b.disabled=false;b.textContent='Use my location';invalidate();scheduleFare(100)}).withFailureHandler(function(){$('pickup').value=pickupLat+', '+pickupLng;b.disabled=false;b.textContent='Use my location';invalidate();scheduleFare(100)}).pulseSmartReverseGeocode({lat:pickupLat,lng:pickupLng,requestToken:requestToken,testMode:testMode})},function(){b.disabled=false;b.textContent='Use my location';error('Location was not shared. Enter the pickup instead.')},{enableHighAccuracy:true,timeout:10000,maximumAge:30000})};
  function collect(){var note=$('notes').value.trim(),marker='[PAY:'+payment+']';return {name:$('name').value.trim(),phone:$('phone').value.trim(),email:$('email').value.trim(),pickup:$('pickup').value.trim(),pickupLat:pickupLat,pickupLng:pickupLng,destination:$('destination').value.trim(),date:today,time:timing==='LATER'?$('time').value.trim():'',timing:timing,paymentPreference:payment,passengers:$('passengers').value,notes:note?marker+' '+note:marker,consent:$('consent').checked,requestToken:requestToken,testMode:testMode,quotedFare:quote&&quote.fare,quoteId:quote&&quote.quoteId,quoteOrigin:quote&&quote.origin,quoteDestination:quote&&quote.destination,quotePickupAt:quote&&quote.pickupAt,quotePricingVersion:quote&&quote.pricingVersion,quoteExpiresAt:quote&&quote.expiresAt,quoteToken:quote&&quote.quoteToken}}
  $('sendBtn').onclick=function(){clearError();if(!currentQuote()){scheduleFare(50);return error('Your fare is recalculating.')}var data=collect();if(!data.name||!data.phone||!data.email||!data.pickup||!data.destination)return error('Complete the name, phone, email, pickup, and destination.');if(data.timing==='LATER'&&!data.time)return error('Choose a time later today.');if(!data.consent)return error('Consent is required.');var b=this;b.disabled=true;b.textContent='Sending…';google.script.run.withSuccessHandler(function(r){$('formView').style.display='none';$('doneView').classList.add('show');$('doneFare').textContent=r&&Number.isFinite(Number(r.quotedFare))?'$'+Number(r.quotedFare).toFixed(2):'';$('donePay').textContent='Payment: Tap to Pay in car'}).withFailureHandler(function(e){b.disabled=false;updateSend();error((e&&e.message)||'The request could not be sent.')}).submitQrLiveRide(data)};
  google.script.url.getLocation(function(route){var params=(route&&route.parameter)||{};requestToken=String(params.request||'');testMode=String(params.mode||'').toLowerCase()==='test';google.script.run.withSuccessHandler(function(c){requestOpen=!!(c&&c.requestOpen);updateSend();if(!re