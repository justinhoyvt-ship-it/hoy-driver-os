# Editable modules

The approved full-page compositions are the locked visual shell. URLs and future feeds are changed independently in `/content`.

- `site-links.json` controls every clickable destination.
- `events.json`, `markets.json`, and `destinations.json` are reserved for live verified feeds.
- Future iframe/feed modules can be mounted in the existing `.live-layer` without replacing the visual shell.
